[package]
name = "karl-sims-core"
version = "0.1.0"
edition.workspace = true

[dependencies]
glam = { version = "0.30", features = ["scalar-math", "serde"] }
serde = { version = "1", features = ["derive"] }
bincode = "1"
rand = "0.8"
rand_chacha = "0.3"
